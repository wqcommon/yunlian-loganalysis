<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunlian.loganalysis.dao.StatOriginLogDataDao">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO stat_origin_logdata VALUES
        <foreach collection="list" item="data" separator=",">
          (
            #{data.uid},
            #{data.remoteAddr},
            #{data.remoteUser},
            #{data.timeLocal},
            #{data.httpHost},
            #{data.request},
            #{data.requestLength},
            #{data.httpReferer},
            #{data.upstreamAddr},
            #{data.status},
            #{data.requestTime},
            #{data.upstreamResponseTime},
            #{data.bodyBytesSent},
            #{data.httpToken},
            #{data.httpAppCode},
            #{data.httpUserAgent},
            now(),
            now()
          )
        </foreach>

    </insert>

</mapper>